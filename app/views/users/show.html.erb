<%= render 'user_profile', user: @user %>

<div>
<h3>Bio</h3>
<p><%= @user.bio %></p>
</div>

<%= render 'posts/posts_summary', post: @post %>

<%= link_to 'See All Posts', posts_user_path(@user), class: 'btn' %>
