<section class="user-section">
    <div class="card">

      <div>
        <div class="post-header">
          <h2>Post # <%= @post.id %> by: <%= @user.name%></h2>
          <ul class="unordered-list likes-comments">
            <li>Likes: <%= @post.likes.count %><i class="material-icons">thumb_up</i></li>
            <li>Comments: <%= @post.comments.count %><i class="material-icons">comments</i></li>
          </ul>
        </div>
        <p><%= @post.text %></p>
      </div>

      <ul class="unordered-list comments-list comments-list-show">
        <% @post.comments.each do |comment| %>
          <li><%= comment.user.name %>: <%= comment.text %></li>
        <% end %>
      </ul>
    </div>
